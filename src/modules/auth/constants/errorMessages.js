const IDENTITY_PROVIDER_IS_REQUIRED = 'Identity provider is required';

const IDENTITY_PROVIDER_IS_INVALID = 'Identity provider %s is invalid';

const USER_NOT_FOUND = 'User not found';

const AUTH_NOT_FOUND = 'Auth not found';

const NOT_AUTHORIZED = 'Not authorized';

module.exports = {
  IDENTITY_PROVIDER_IS_REQUIRED,
  IDENTITY_PROVIDER_IS_INVALID,
  USER_NOT_FOUND,
  AUTH_NOT_FOUND,
  NOT_AUTHORIZED
};
